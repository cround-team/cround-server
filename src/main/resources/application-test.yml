spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    root: info
#    org:
#      apache:
#        coyote:
#          http11: debug

#decorator:
#  datasource:
#    p6spy:
#      enable-logging: true
